import { PlayerIndexPipePipe } from './player-index-pipe.pipe';

describe('PlayerIndexPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PlayerIndexPipePipe();
    expect(pipe).toBeTruthy();
  });
});
