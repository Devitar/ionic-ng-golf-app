import { InputCellPipe } from './input-cell.pipe';

describe('InputCellPipe', () => {
  it('create an instance', () => {
    const pipe = new InputCellPipe();
    expect(pipe).toBeTruthy();
  });
});
