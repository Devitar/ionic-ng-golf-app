import { FormConversionPipe } from './form-conversion.pipe';

describe('FormConversionPipe', () => {
  it('create an instance', () => {
    const pipe = new FormConversionPipe();
    expect(pipe).toBeTruthy();
  });
});
